# Instrucciones para Claude Code

Las instrucciones del sitio AEMATEC están en `AGENTS.md`, compartidas con otros agentes de IA. Se incluyen aquí:

@AGENTS.md

## Solo para Claude Code
- Las guías de `.claude/skills/` son skills: úsalas con la herramienta Skill (`publicar-cambios`,
  `traspaso-de-junta`) cuando el tema lo pida.
- Si cambias una regla o un paso de trabajo, edítalo en `AGENTS.md`, no aquí, para que otros agentes también
  lo sigan.
