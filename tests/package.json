{
  "name": "aematec-rules-tests",
  "private": true,
  "description": "Pruebas de las reglas de Firestore y Storage con el emulador de Firebase.",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,storage --project demo-aematec \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.11.0"
  }
}
